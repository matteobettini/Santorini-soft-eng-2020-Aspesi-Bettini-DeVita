package it.polimi.ingsw.common.enums;

/**
 * The possible types of actions requested by the server to the players
 */
public enum ActionType {
    CHOOSE_START_PLAYER,
    SET_WORKERS_POSITION,
    MOVE,
    BUILD,
    MOVE_BUILD
}
